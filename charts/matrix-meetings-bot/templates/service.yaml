apiVersion: v1
kind: Service
metadata:
  name: {{ include "matrix-meetings-bot.fullname" . }}
  labels:
    {{- include "matrix-meetings-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "matrix-meetings-bot.selectorLabels" . | nindent 4 }}
